services:
  backenddev:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    volumes:
      - .:/code
    ports:
      - ${BACKEND_PORT}:${BACKEND_PORT}
    depends_on:
      - redis
  redis:
    image: "redis:alpine"
    ports:
      - ${REDIS_PORT}:6379
